type Query {
  tasks: [Task!]!
}
type Task {
  _id: ID!
  title: String!
  description: String!
  completed: Boolean!
  createdAt: String!
}

type Mutation {
  addTask(
    title: String!
    description: String!
    completed: Boolean!
    createdAt: String!
  ): Task
}
